{/* Promise — the thesis in one breath, renamed from Statement (P3-03, §2.5).
	The pause between the pipeline and the work, not a second star moment. */}
<section id="promise" data-section-label="Promise" aria-labelledby="promise-heading">
	<div class="wrap">
		<h2 id="promise-heading" class="sr-only">Promise</h2>
		<p class="project-kicker">My promise</p>
		<p class="promise-sentence" aria-hidden="true">
			Every data process I take on runs lean and fluid —{' '}
			<span class="accent-words">optimized end to end</span>, answered on time, delivered with
			quality.
		</p>
		<p class="sr-only">
			Every data process I take on runs lean and fluid — optimized end to end, answered on time,
			delivered with quality.
		</p>
	</div>
</section>

<style>
	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0 0 0 0);
		clip-path: inset(50%);
		white-space: nowrap;
		border: 0;
	}

	.promise-sentence {
		max-width: 24ch;
		font-family: var(--font-display);
		font-weight: 600;
		font-size: clamp(1.9rem, 5vw, 4.5rem);
		line-height: 1.08;
		letter-spacing: -0.015em;
		color: var(--text-primary);
		text-wrap: balance;
		margin-top: var(--space-4);
	}

	/* This viewport's single amber focus — always amber in reduced-motion/no-JS;
	   the scrub-reveal in promise.ts lights it progressively on scroll. */
	.accent-words {
		color: var(--accent);
	}
</style>
